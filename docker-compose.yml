services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: localbuddy
      POSTGRES_PASSWORD: localbuddy
      POSTGRES_DB: localbuddy
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
