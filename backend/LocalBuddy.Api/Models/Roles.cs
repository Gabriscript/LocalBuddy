namespace LocalBuddy.Api.Models;

/// Identity roles, as opposed to User.Role which is the domain role (host / guest / entrambi).
public static class Roles
{
    public const string Moderator = "moderator";
}
